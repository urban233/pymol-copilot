# M-01 evidence — chain-A/red gold-case verifier

**Status:** Merged. M-A1 through M-A8 recorded by Martin. Bounded program-first generation added after initial acceptance (developer-authorized); see that section and its outer-loop re-review below. Hannah's own GitHub PR review went through two rounds (`CHANGES_REQUESTED`, 4 comments, then `CHANGES_REQUESTED` again, 3 more comments) -- both addressed comment by comment, see those sections below. Machine review complete (`ok_machine_review_complete`), PR re-marked ready, then approved by Hannah Kullik (`kullik01`) at head commit `8cce4cac2e21b85b8fd5c1db5a6895edf66e535b` on 2026-09-07T22:50:28Z; PR #13 merged as `59748e677a97b54432e857c4307d78369badeec7`. M-R1 satisfied. See "W2-00 discharges this item's cross-cutting flag" below: the real-PyMOL exit-code defect this item found in `tests/integration/test_real_pymol_command.py` (H-01's file, flagged but not fixed here) is now fixed.
**Item:** M-01, Implement a chain-A/red gold-case verifier
**Owner:** Martin Urban (`urban233`)
**Independent reviewer:** Hannah Kullik (`kullik01`)
**Wave plan:** [PyMOL-Copilot wave plan](../pymol-copilot.md#martins-acceptance-checklist)
**Tracker:** Partial delivery of [#8](https://github.com/urban233/pymol-copilot/issues/8); issue stays open for generation work

## Snapshot

- Base commit: `09cd7eb632c756809c4fff0486cac51a1883a050` (branch `codev/M-01`)
- Tested head commit: `6e0946d52305c6ede037a8560a5e89029296fd23` (this is the exact
  snapshot the repository-wide checks and CI green run below were run
  against; see the CI note and the outer-loop finding section for what
  changed since the implementation commit)
- Source paths: `src/pmc_data/pdb.py`, `src/pmc_data/oracle.py`, `src/pmc_data/gold_case.py`, `src/pmc_data/verifier.py`, `src/pmc_data/gold_cases/chain_a_red.json`, `src/pmc_data/BUILD.bazel`, `tests/data/*`
- `git status --short`: clean at the tested head (no unrecorded source/test changes)
- Issue/PR link: [#8](https://github.com/urban233/pymol-copilot/issues/8) (narrowed to M-01's bounded scope before implementation); no pull request opened yet

## Environment

- OS/architecture: macOS, darwin/arm64
- Python: 3.13.15 (Bazel-pinned interpreter reports 3.13.13 inside the sandbox; `python3 --version` on the host reports 3.13.15)
- Open-Source PyMOL (pinned): `pymol-open-source-whl==3.2.0.2`
- Relevant dependencies: Bazelisk 1.29.0 / Bazel 9.2.0; no new Python dependency added
- Environment/install recipe: [`docs/development_setup.md`](../../development_setup.md); no changes required for this task
- Probe command and result: `bazel test //tests/integration:real_pymol_command --lockfile_mode=error` → PASSED (1/1), confirming the existing real-PyMOL environment used by H-01 also works for M-01 without a new PyMOL/Python combination
- Finite test deadlines: the real-PyMOL target (`//tests/data:gold_case_verifier`) is `size = "large"`, `timeout = "moderate"` (Bazel-enforced), matching `//tests/integration:real_pymol_command`'s convention

## Fixture and contracts

- Controlled structure path: `tests/data/testdata/chain_a_gold_fixture.pdb`
- Public/licensed source: self-authored synthetic development structure created for this repository; not derived from any external or copyrighted source; public domain (CC0-equivalent), same convention as the existing `tests/integration/testdata/two_chain_fixture.pdb`. Asymmetric chain split (chain A: 3 atoms, chain B: 2 atoms) so a wrong-chain case is unambiguous.
- SHA-256 verification output: `0f2cd04ed9d52ce106dee4a2e5b3384b8d755dd7b818e4930d9e6778e52fc5dd` (`shasum -a 256 tests/data/testdata/chain_a_gold_fixture.pdb`), matching the checksum recorded in `src/pmc_data/gold_cases/chain_a_red.json`
- Fixture identity: gold record `case_id: "chain_a_red_gold_case"`; PyMOL object name `chain_a_gold_fixture`; independently-derived stable atom identity is the PDB atom serial number, which is verified equal to PyMOL's own per-atom `index` for this contiguous, single-model, no-altloc fixture (both are 1..5 in file order)
- Canonical PML bytes: `select copilot_selection, chain A\ncolor red, copilot_selection\n`, produced by `pmc_core.plan.initial_fixture_plan().render_pml()` and cross-checked byte-for-byte against the gold record at load time (`GoldCase.__post_init__` rejects any drift)
- Plan/policy versions: `pmc_core.plan`/`pmc_core.policy` consumed unchanged (no version constant exists in those modules; the gold record's `contract_versions.plan_version` records `"1"`, the one shared version identifier already used across `pmc_core.protocol`'s plan/policy/snapshot fields)
- Other contract versions actually used: `contract_versions.pymol_version: "3.2.0.2"` (matches `requirements.in`'s pin); no runtime/client-server contract is exercised by this item (not implemented here, by design — M-01 does not touch `pmc_client`/`pmc_server`)

## Checklist

- [x] **M-A1: Reproducible fixture and environment.** The controlled fixture, its self-authored provenance note, and verified SHA-256 are recorded above. The pinned PyMOL environment probe passed (`//tests/integration:real_pymol_command`), and `//tests/data:gold_case_verifier`'s `test_true_gold_case_passes_every_assertion` is itself a successful disposable real-PyMOL reference invocation against `tests/data/testdata/chain_a_gold_fixture.pdb` (not a placeholder digest).
- [x] **M-A2: Complete gold record and lossless round-trip.** `src/pmc_data/gold_cases/chain_a_red.json` carries stable ID, source/checksum, intent, category, difficulty, typed/canonical plan, assertions, and contract/PyMOL versions. `tests/data/test_gold_case.py::test_gold_case_round_trip_preserves_provenance_assertions_and_plan` proves `to_dict`/`from_dict` preserves provenance, assertions, and canonical plan identity.
- [x] **M-A3: Independent selection oracle.** `src/pmc_data/oracle.py`/`pdb.py` derive expected atom identities directly from the structure file's chain field (never a PyMOL selection); `tests/data/test_oracle.py` asserts the expected chain A `{1,2,3}` and chain B `{4,5}` sets by direct inspection of the controlled file. The real-PyMOL differential run in `test_true_gold_case_passes_every_assertion`'s `chain_membership` `AssertionResult.detail` records both the oracle-expected and PyMOL-actual sets (`expected=[1, 2, 3] actual=[1, 2, 3]`), so expected/actual/count agreement is retained per-run, not asserted from equality alone.
- [x] **M-A4: Correct-case assertions.** `tests/data/test_gold_case_verifier.py::test_true_gold_case_passes_every_assertion` shows exact chain-A membership, requested red color on target atoms, and unchanged chain-B color state, with `TaskSuccess` true only because all three assertions pass.
- [x] **M-A5: Semantic negative cases.** Separate `test_wrong_chain_case_fails_chain_membership`, `test_wrong_color_case_fails_color_state`, and `test_unintended_change_is_detected` each corrupt one declared expectation (or inject a real mutation via `inject_after_execution`) and assert the relevant assertion fails while the others still pass, and that `TaskSuccess` is never true.
- [x] **M-A6: Invalid cases fail closed.** `test_gold_case.py` covers missing-provenance/missing-required-field and unsupported-assertion-kind rejection at construction time (8 parametrized missing-field cases plus dedicated provenance/kind/plan-drift tests). `test_gold_case_verifier.py::test_missing_assertion_param_invalidates_the_result` and `::test_real_pymol_evaluator_error_invalidates_the_result` cover a missing assertion parameter and a real-PyMOL-surfaced evaluator error (an assertion naming a color `cmd.get_color_index` does not recognize) respectively; both set `valid=False` with an empty `assertion_results` and a diagnostic `invalid_reason`, never a passing or defaulted score.
- [x] **M-A7: Sabotage detects an incorrect oracle.** `tests/data/test_oracle_sabotage.py` copies `pmc_data` to a disposable temp directory, replaces `oracle.expected_chain_atom_ids`'s body with `return frozenset()`, runs the copied `test_oracle.py` suite as a subprocess, and asserts it fails naming `test_expected_chain_a_atom_ids_match_the_known_fixture_layout`. The sabotage patch exists only inside the temporary directory; the handed-off `src/pmc_data/oracle.py` is unmodified and its own clean run passes (`bazel test //tests/data:oracle`).
- [x] **M-A8: Complete implementation evidence.** Runnable verifier: `src/pmc_data/verifier.py:verify_gold_case`. New test targets and results are in Commands and results below. `pmc_core.plan`/`pmc_core.policy` are imported and consumed unchanged (no edits to `src/pmc_core/`). Tested scope is exactly the one `chain_a_red_gold_case` gold record against `tests/data/testdata/chain_a_gold_fixture.pdb`; no disagreement was observed or filtered. Issue #8's generation portion remains explicitly open (see its narrowed body); this receipt makes no production-executor or broad-generalization claim.
- [x] **M-R1: Hannah accepts the handoff evidence.** PR #13 approved by
  Hannah Kullik (`kullik01`) at head commit
  `8cce4cac2e21b85b8fd5c1db5a6895edf66e535b` on 2026-09-07T22:50:28Z, after
  two `CHANGES_REQUESTED` rounds (both addressed comment by comment, see
  below); merged as `59748e677a97b54432e857c4307d78369badeec7`.

## Commands and results

| Checklist ID | Test target / test name | Exact command | Result | Log / artifact |
|---|---|---|---|---|
| M-A1 | `//tests/integration:real_pymol_command` | `bazel test //tests/integration:real_pymol_command --lockfile_mode=error` | PASSED, 1/1 | Bazel test log |
| M-A1, M-A3, M-A4 | `//tests/data:gold_case_verifier::test_true_gold_case_passes_every_assertion` | `bazel test //tests/data:gold_case_verifier --lockfile_mode=error --test_output=all` | PASSED | Bazel test log; per-assertion detail strings captured in test stdout |
| M-A2 | `//tests/data:gold_case` (13 cases: round-trip, 8 missing-field, provenance-checksum, unsupported-kind, bare-Assertion rejection, canonical-plan-drift) | `bazel test //tests/data:gold_case --lockfile_mode=error --test_output=all` | PASSED, 13/13 | Bazel test log |
| M-A3 | `//tests/data:oracle` (3 cases: chain A, chain B, absent chain) | `bazel test //tests/data:oracle --lockfile_mode=error --test_output=all` | PASSED, 3/3 | Bazel test log |
| M-A5 | `//tests/data:gold_case_verifier::test_wrong_chain_case_fails_chain_membership`, `::test_wrong_color_case_fails_color_state`, `::test_unintended_change_is_detected` | `bazel test //tests/data:gold_case_verifier --lockfile_mode=error --test_output=all` | PASSED (each fails only its targeted assertion) | Bazel test log |
| M-A6 | `//tests/data:gold_case_verifier::test_missing_assertion_param_invalidates_the_result`, `::test_real_pymol_evaluator_error_invalidates_the_result`; `//tests/data:gold_case` (missing-field/kind/drift cases) | `bazel test //tests/data:gold_case_verifier //tests/data:gold_case --lockfile_mode=error --test_output=all` | PASSED | Bazel test log |
| M-A7 | `//tests/data:oracle_sabotage::test_oracle_suite_rejects_a_corrupted_chain_membership_derivation` | `bazel test //tests/data:oracle_sabotage --lockfile_mode=error --test_output=all` | PASSED (subprocess run against the sabotaged copy failed as expected; the un-sabotaged handed-off code passes separately via `//tests/data:oracle`) | Bazel test log |
| Repository-wide | `//...` build | `bazel build //... --lockfile_mode=error` | PASSED | Bazel build log |
| Repository-wide | `//...` tests | `bazel test //... --lockfile_mode=error` | PASSED, 15/15 test targets (10 run, 5 cached) | Bazel test log |
| Repository-wide | dependency boundaries | `bazel run //tools/bazel:check_dependency_boundaries --lockfile_mode=error` | PASSED (exit 0); `pmc_core`/`pmc_agent` closures remain free of `pmc_data`/`pmc_train` | Command output |
| Repository-wide | lint | `bazel run //tools/quality:ruff --lockfile_mode=error -- check .` | PASSED, "All checks passed!" | Command output |
| Repository-wide | format | `bazel run //tools/quality:ruff --lockfile_mode=error -- format --check .` | PASSED, 83 files formatted | Command output |
| Repository-wide | types | `bazel run //tools/quality:pyrefly --lockfile_mode=error -- check` | PASSED, 0 errors | Command output |

All commands were run from the repository root on branch `codev/M-01` at commit `c9c0b1b17fe8bcfe63cb7a33833ea15d8ffae0e0`. No test was skipped.

## Observations

- Chain-membership differential: expected (independent oracle) `{1, 2, 3}` for chain A and `{4, 5}` for chain B; actual (real PyMOL `copilot_selection` membership after `select copilot_selection, chain A`) `{1, 2, 3}`; zero difference on the true gold case. The wrong-chain negative case's declared expectation (`{4, 5}`, chain B) differs from the real, unchanged PyMOL selection (`{1, 2, 3}`), and the verifier reports that mismatch as a failed assertion.
- Color state: real PyMOL colors `copilot_selection` (chain A, 3 atoms) with the fixture's `red`; `cmd.get_color_index("red")` matches every captured atom color index on the true case, and does not match on the wrong-color negative case (declared expectation `blue`).
- Unintended-change comparison scope: chain B's per-atom `(index, color)` pairs, captured immediately after fixture load and again after the plan executes (and after any injected mutation). The true case shows zero difference; the injected `color blue, chain B` mutation used by `test_unintended_change_is_detected` produces an observed difference, and the assertion correctly fails only that one check while chain-membership and color-state on the target selection still pass.
- Real-PyMOL evaluator-error finding: malformed PyMOL selection syntax (e.g. `"(((not valid"`) does **not** raise a Python exception from `cmd.iterate`/`cmd.count_atoms` in this pinned PyMOL build -- it prints a `Selector-Error` diagnostic to PyMOL's own console and the selection simply matches zero atoms. `cmd.get_color_index` on an unrecognized color name returns `-1` rather than raising. The verifier therefore treats a negative color index as the deterministic, real-PyMOL-surfaced error condition for M-A6's "evaluator error" case; this was confirmed empirically with a disposable debug probe against real PyMOL before being encoded as the negative test.
- Comparison scope and limitations: this receipt's real-PyMOL comparison covers selection membership (by atom index) and per-atom color index only, for the one fixed `select`/`color` fixture. It does not compare coordinates (unchanged by this fixture's operations by construction) or claim whole-session fidelity.
- Conformance disagreements: none observed or filtered. Every assertion result above is reported, including the three deliberately-corrupted negative cases' expected failures.

## Style audit

A `code-audit-gate` pass ran against this exact change before the lightweight-reviewer's `READY_FOR_OUTER_LOOP` decision was recorded. It applied and this receipt accepts: removing Sphinx-style backticks from docstrings (repo-style-checker rule `docstring-markup`), renaming the `ChainColorSnapshot` type alias to `CHAIN_COLOR_SNAPSHOT` to match `pmc_core.plan`'s existing `OPERATION` type-alias convention (`binding-naming`), collapsing one multi-line comment into a single complete sentence (`comment-punctuation`), and adding a docstring to one nested test helper (`function-docstring`).

Three categories of finding from `.agents/skills/audit-google-python-style/scripts/check_google_rules.py` were deliberately **not** applied, because they are pre-existing, repo-wide, already-tolerated patterns rather than something introduced by this task -- confirmed by running the checker against the full repository and finding the same rule already violated in already-merged code:
- `import-class-not-module` (e.g. `from pathlib import Path`, `from pmc_core.plan import SelectOperation`): already present in `src/pmc_core/protocol.py`. Rewriting only this task's new files would make them diverge from every other file's import style rather than align with it.
- `docstring-args` on ordinary pytest fixture-taking test functions with a one-line docstring: already the convention in the already-merged `tests/integration/test_real_pymol_command.py` (H-01), which this task's real-PyMOL test module directly follows.
- `docstring-raises` flagged on `verify_gold_case`: a false positive from the checker's lack of exception-flow analysis -- the function's own `try`/`except` guarantees it never actually raises to its caller (it always returns a `VerifierResult`), so adding a `Raises` section would misdescribe its contract.

## CI note (outer-loop gate)

PR #13's first CI run (commit `222be7139b4989913d04246ee45a65917f2e63d8`) failed only on `smoke (windows-2025)`, in the pre-existing `tests/integration:command` target -- untouched by this PR's diff -- with `FileNotFoundError: [WinError 2] ... local-spawn-runner.<id>` raised during pytest collection. This is a distinct signature from the already-known, already-resolved issue #12 (`ImportError: DLL load failed ... filename too long`), confirmed by comparing against H-01's historical CI failure logs. `main`'s Windows job passed cleanly about an hour earlier at the same toolchain/dependency versions.

Working hypothesis, not fully proven: Windows CI runs Bazel with `--spawn_strategy=local` (no sandbox isolation), and the failing run's log shows 4 concurrent local actions at the moment of failure, including `//tests/data:oracle_sabotage`. That target follows the same pattern as the pre-existing `tests/contract:policy_sabotage` -- copy a package into a disposable `tempfile.TemporaryDirectory()`, then spawn a nested `pytest` subprocess. Before M-01, only one such subprocess-spawning sabotage test existed; M-01 added a second, increasing the chance of concurrently scheduled subprocess-spawning tests colliding under Windows's unsandboxed local execution.

Mitigation applied (developer-approved, expanding M-01's allowed scope to touch one existing file): added `tags = ["exclusive"]` to both `tests/contract:policy_sabotage` and `tests/data:oracle_sabotage`, so Bazel never schedules either concurrently with another test. Re-validated locally: full `bazel build //...` and `bazel test //...` pass (15/15), dependency boundaries pass, ruff check/format pass, pyrefly passes (0 errors).

CI re-run on the pushed commit (`19eead53340597be696a29113f98be101e69b13a`, run [34056923197](https://github.com/urban233/pymol-copilot/actions/runs/34056923197)) came back green on all three platforms (windows-2025, ubuntu-24.04, macos-15), including `tests/integration:command` on Windows. This is a positive but probabilistic signal, not proof the hypothesis was exactly right -- a single green run cannot fully rule out an unrelated one-off flake -- but it is consistent with the mitigation, and no further Windows failure has recurred.

## Outer-loop finding: real-PyMOL test target's exit code could not be trusted (fixed)

The `concurrency-specialist` outer-loop reviewer found, and I independently confirmed by empirical probe, that `//tests/data:gold_case_verifier` could report Bazel `PASSED` even when a test inside it genuinely failed. Root cause: real headless PyMOL's process-teardown path (triggered by its own internal cleanup during normal Python interpreter shutdown, not specifically by the explicit `cmd.do("quit")` call -- removing that call alone did not fix it) can complete *after* the point a correct process would have already exited, and its own exit path forces process exit code 0, silently overriding whatever real pytest result (0 or 1) was already computed.

**Confirmed empirically, twice:**
- Baseline control: a deliberately failing assertion added to a non-PyMOL target (`//tests/data:gold_case`) correctly reported Bazel `FAILED` -- ruling out a generic Bazel/rules_python exit-code bug.
- Before the fix: the same deliberate failure added to `//tests/data:gold_case_verifier` reported Bazel `PASSED` (`1 failed, 5 passed` in the pytest log, `Executed 1 out of 1 test: 1 test passes` from Bazel) -- reproducing the defect exactly as the specialist described, with and without the explicit `cmd.do("quit")` call present.
- After the fix: the same deliberate failure correctly reported Bazel `FAILED` (`Exit 1`), and removing the probe again correctly reported `PASSED` -- both directions verified, not just the failure case.

**Fix applied** (in `tests/data/test_gold_case_verifier.py`, within M-01's original allowed scope): the module's `if __name__ == "__main__":` entry point now calls `os._exit(pytest.main([__file__]))` instead of `raise SystemExit(pytest.main([__file__]))`. `os._exit` terminates the process immediately with pytest's real result, without running the interpreter-shutdown machinery real PyMOL's cleanup piggybacks on.

A narrow re-verification by the concurrency specialist against the fixed head confirmed the exit-code fix resolves the original finding (empirically reproduced: a deliberate failure now correctly reports Bazel `FAILED`), and surfaced one more real, non-blocking gap: `os._exit` also skips the normal stdio flush, so a genuine failure's traceback and summary could be silently truncated or lost from the captured test log (confirmed empirically: the log ended mid-progress-line with the failure details missing). Fixed by flushing `sys.stdout`/`sys.stderr` explicitly before `os._exit`; re-verified empirically that the full failure report now survives in the log, and that the clean suite still reports `PASSED`.

**Cross-cutting concern, not fixed here:** the already-merged `tests/integration/test_real_pymol_command.py` (H-01, `pmc_client`/`pmc_server` lane) uses the identical `pymol.finish_launching`/`cmd.do("quit")`/`raise SystemExit(pytest.main(...))` pattern and is very likely affected the same way -- meaning that target's own green CI history may not be fully trustworthy either. This file is outside M-01's allowed scope and owned by Hannah's H-01 lane per the wave plan's file ownership; fixing it here would cross that boundary without her involvement. Flagging this explicitly as a needed follow-up (a new tracked issue, or a message to Hannah) rather than silently leaving it or silently fixing someone else's file.

## Two non-blocking concurrency notes closed at developer request

After the outer-loop round recorded `READY_FOR_HUMAN_APPROVAL`, the developer asked to close the two remaining non-blocking `concurrency` findings from the merged outer-loop report before human review:

- **`sabotage-copytree-race-only-bazel-enforced`:** both sabotage tests' `shutil.copytree` of the shared package could race a concurrent process writing into it; the only existing protection is Bazel's `tags = ["exclusive"]` at the BUILD level, invisible from the test itself. Added a comment to both `tests/data/test_oracle_sabotage.py` and `tests/contract/test_policy_sabotage.py` recording that the protection is build-graph-level and does not apply outside `bazel test`.
- **`windows-tempdir-cleanup-permission-error-risk`:** bare `tempfile.TemporaryDirectory()` cleanup right after a just-exited child process wrote into the tree is a known intermittent Windows `PermissionError` source. Added `ignore_cleanup_errors=True` to both sabotage tests' `TemporaryDirectory()` call.

This touches `tests/contract/test_policy_sabotage.py`, a pre-existing shared-core test file outside M-01's original allowed scope (same file pair as the earlier `tags = ["exclusive"]` mitigation) -- developer-approved, comment-and-flag-only change, no behavior change to what either sabotage test proves. Re-validated: full `bazel build //...`/`bazel test //...` (15/15), ruff check/format, and pyrefly all pass at the new head.

## Bounded program-first generation (developer-authorized scope expansion)

After the outer-loop round reached `READY_FOR_HUMAN_APPROVAL`, the developer
asked to deliver the wave plan's own M-01 Completion-limit shortcut now,
inside this same PR: "Additional examples can use the existing command
fixture on different controlled structures." M-01's Containment rule (no
teacher access, no production execution path) rules out the design doc's
full program-first pipeline (teacher back-translation, curation, a hermetic
executor -- none of which exist in this repository), so the delivered scope
is bounded to exactly that shortcut, confirmed with the developer before
implementation:

- **`src/pmc_data/generate.py`:** `GenerationRequest`/`GenerationResult`,
  `build_candidate_gold_case` (reuses `pmc_core.plan.initial_fixture_plan()`
  unchanged; only provenance/identity/non-target chains vary by structure),
  `generate_gold_case` (verifies the candidate through the existing real-
  PyMOL `verify_gold_case` path), and `write_generated_gold_case`, which
  raises `GenerationRejectedError` rather than persisting anything not both
  `valid` and `task_success`.
- **`src/pmc_data/generate_cli.py`:** the one file in `pmc_data` that
  imports real PyMOL directly (every other module takes a caller-supplied
  `cmd`, the same separation `verifier.py` already keeps); launches PyMOL
  once, runs every request in the checked-in config, and writes only
  verified results.
- **`configs/generation/chain_a_red_structures.json`** (+ `BUILD.bazel`
  `exports_files`): the one generation request that produced the new gold
  record below.
- **New fixtures:** `tests/data/testdata/second_gold_fixture.pdb` (chain A:
  5 atoms, chain C: 4 atoms; SHA-256
  `a91cfcaa3f6ac283fd7a7437def56fb3081c52b482390cd93607c5b2b7f5606a`), used
  for the successful-generation case; `tests/data/testdata/no_chain_a_fixture.pdb`
  (chain Z only, no chain A at all), used only for the rejection case.
- **New gold record:** `src/pmc_data/gold_cases/chain_a_red_second_fixture_gold_case.json`,
  produced by actually running `bazel run //src/pmc_data:generate_cli
  --lockfile_mode=error` against real PyMOL (not hand-authored) -- output:
  `PyMOL>select copilot_selection, chain A` / `Selector: selection
  "copilot_selection" defined with 5 atoms.` / `PyMOL>color red,
  copilot_selection` / `Executive: Colored 5 atoms.` / `WROTE
  .../gold_cases/chain_a_red_second_fixture_gold_case.json`.
- **`tests/data/test_generate.py`** (pure Python, no PyMOL): config loading
  resolves `structure_relpath` correctly; the candidate reuses
  `initial_fixture_plan().render_pml()` byte-for-byte; the checksum matches
  an independently computed SHA-256; `write_generated_gold_case` refuses an
  unverified `GenerationResult` and persists a verified one as
  round-trippable, indented JSON.
- **`tests/data/test_generate_real_pymol.py`** (real PyMOL, Windows-excluded
  like `gold_case_verifier`, issue #12): regenerating the checked-in second
  gold case from the same request reproduces it exactly (`result.gold_case
  == GoldCase.from_json_file(...)`), proving it is reproducible rather than
  hand-edited; a request against the no-chain-A fixture is rejected by an
  independent pre-check before any PyMOL selection or color assertion runs
  (`valid=False`, `assertion_results=()`, `invalid_reason` naming chain
  `'A'`), rather than silently written as a false positive. (This pre-check
  was added during outer-loop review -- see below; the fixture was
  originally caught only incidentally via a failing `color_state`
  assertion.)

Commands and results:

| Target | Command | Result |
|---|---|---|
| `//tests/data:generate` | `bazel test //tests/data:generate --lockfile_mode=error --test_output=all` | PASSED |
| `//tests/data:generate_real_pymol` | `bazel test //tests/data:generate_real_pymol --lockfile_mode=error --test_output=all` | PASSED |
| Repository-wide build | `bazel build //... --lockfile_mode=error` | PASSED |
| Repository-wide tests | `bazel test //... --lockfile_mode=error` | PASSED, 17/17 test targets |
| Dependency boundaries | `bazel run //tools/bazel:check_dependency_boundaries --lockfile_mode=error` | PASSED; `pmc_core`/`pmc_agent` closures unaffected |
| Lint | `bazel run //tools/quality:ruff --lockfile_mode=error -- check .` | PASSED, "All checks passed!" |
| Format | `bazel run //tools/quality:ruff --lockfile_mode=error -- format --check .` | PASSED, 87 files formatted |
| Types | `bazel run //tools/quality:pyrefly --lockfile_mode=error -- check` | PASSED, 0 errors |

## Outer-loop re-review of the generation expansion

The task was reopened (`codev task reopen`) for this expansion and went
through three specialists (`correctness-tests-specialist`,
`security-data-specialist`, `architecture-maintainability-specialist`;
`concurrency` and `rollout` waived -- no new subprocess/threading surface,
no production consumer). `security_privacy_data_compatibility`,
`architecture_scope`, and `maintainability` all passed on the first pass
(3, 0, and 8 non-blocking notes respectively, none required to fix).
`correctness`, `error_handling`, and `test_quality` did not:

1. **Blocking:** `generate_cli.py` documented "exits non-zero if any
   request was rejected" but used `raise SystemExit(run())` -- the identical
   PyMOL-shutdown exit-code-override defect already fixed once in
   `test_gold_case_verifier.py`, just not carried to the new file.
   Reproduced empirically (rejected generation printed `REJECTED ...` but
   exited 0). **Fixed** with the same `os._exit` + explicit flush pattern;
   re-verified empirically in both directions (exit 1 on rejection, exit 0
   on success), including a control run of the pre-fix commit in the same
   environment to confirm the reproduction wasn't a harness artifact.
2. **Non-blocking, developer chose to fix anyway** (both because they were
   real and because `error_handling`/`test_quality` stayed "not passed"
   until closed, which blocked `codev task check`'s coverage gate):
   verification could pass vacuously when a declared chain (target or
   non-target) has no atoms in the structure, since an absent chain's
   before/after snapshots are both empty and `no_unintended_change`
   trivially passes -- reproduced with a typo'd non-target chain "Q" being
   written into a "verified" record. **Fixed:** `build_candidate_gold_case`
   now validates every declared chain actually has atoms, via the same
   independent oracle, before ever touching PyMOL; `generate_gold_case`
   short-circuits such a request without calling `cmd` at all (proved with a
   stub whose every method raises if called). `_request_from_dict` also
   tightened to reuse `gold_case.py`'s own required-field validation instead
   of bare `assert`/`str()` coercion; the no-chain-A rejection test
   strengthened to assert the specific pre-check outcome instead of an
   ambiguous "nothing loaded" one; the config-cardinality coupling in both
   test files removed (select by `case_id`, not `len(requests) == 1`).
3. **Discovered and fixed within the same round:** the absent-chain
   pre-check above, by construction, removed the only test that reached
   `generate_gold_case`'s core "never promote a candidate whose real
   verification failed" guard with a genuinely failing verification --
   confirmed by the specialist enumerating every call site and showing a
   mutation of that guard to always-promote would leave all tests green.
   **Fixed:** added a fake `PyMOLCmd` (`_WrongColorCmd`) that resolves real
   chain atoms via the same independent oracle and genuinely evaluates the
   verifier's per-atom `iterate` expressions, but reports a color index that
   deliberately mismatches `get_color_index("red")`, driving a real
   `valid=True`/`task_success=False` result with no PyMOL involved.
   Independently mutation-tested twice (once by me, once redone
   independently by the specialist): mutating the guard to always-promote
   makes exactly this new test fail; restoring it makes the suite pass again.

Final coverage after three re-verification passes: `correctness`,
`error_handling`, and `test_quality` all `passed: true`; `codev task check`
returned `ok_machine_review_complete` and the PR was re-marked ready. No
finding from this round remains open.

## Hannah's GitHub PR review, addressed comment by comment

Hannah left an actual GitHub PR review (not a codev outer-loop round) --
`CHANGES_REQUESTED` at commit `47a685d7246165c0732a1e2a684915365ccb4e05`,
with 4 inline comments (2 blocking, 2 non-blocking). Processed through the
outer-loop-review skill's comment-sourced entry mode: each comment drafted
directly into a finding, task reopened, all four triaged `address` at the
developer's explicit instruction, fixed in one commit
(`7c2e37957ddeb383f71e39b3e17c3d24b9cadb0a`), independently re-verified by
`lightweight-reviewer`, and replied to individually on GitHub.

| Comment | Location | Severity | Fix |
|---|---|---|---|
| `PR13-VERIFIER-SYNC-RACE` | `verifier.py:297` | Blocking | `cmd.sync()` added after the operations loop and after `inject_after_execution`, plus added to the `PyMOLCmd` Protocol -- headless PyMOL's command loop runs on its own worker thread, so `do()` only enqueues a command; without `sync()`, a read-after-write race can mis-grade a genuinely passing or failing run. Confirmed real PyMOL's `cmd.sync()` exists and works via a disposable probe test. |
| `PR13-PDB-MALFORMED-RECORD` | `pdb.py:54` | Blocking | `read_atoms` now raises `MalformedPdbRecordError` for a line too short to contain the chain-id column (previously silently produced `chain_id=""`, dropping that atom from every chain's expected set). Added the promised `tests/data/test_pdb.py`. |
| `PR13-VACUOUS-PASS` | `verifier.py:247` | Non-blocking | `_evaluate_chain_membership` and `_evaluate_no_unintended_change` now raise `VerifierError` when the relevant independent-oracle expectation or captured snapshot is empty, instead of a vacuous pass on two empty sides -- fixed in the core verifier itself (distinct from the earlier generator-level pre-check, which only protects requests built through `generate.py`). |
| `PR13-PROVENANCE-UNVERIFIED` | `verifier.py:271` | Non-blocking | `verify_gold_case` now recomputes `structure_path`'s actual SHA-256 and cross-checks it against `case.provenance.structure_sha256` before grading, raising on mismatch. |

All four have a dedicated new regression test. Full validation at the fix
commit: `bazel build //...` and `bazel test //...` pass (18/18 targets,
including new `//tests/data:pdb` and 3 additional tests in
`//tests/data:gold_case_verifier`), ruff check/format, pyrefly, and
dependency boundaries all pass. `codev task check` returned
`ok_machine_review_complete`; the PR was re-marked ready and each comment
thread received an individual GitHub reply summarizing its fix.

## Hannah's second GitHub PR review, also addressed comment by comment

Hannah re-reviewed the fix commit and left a second `CHANGES_REQUESTED`
review at `bb5fe47708180a05a114c50a37a3071ede98dcdd`, with 3 more comments.
Same comment-sourced entry-mode process as the first round: drafted,
triaged `address`, fixed in one commit
(`4b0de2391b9532a5d1d5136bc1e242fa6421304f`), independently re-verified by
`lightweight-reviewer` (which redid every mutation test itself), and
replied to individually.

| Comment | Location | Severity | Fix |
|---|---|---|---|
| `PR13-ATOM-IDENTITY-INDEX-VS-SERIAL` | `verifier.py:161` | P2 (newly discovered, critical) | The independent oracle compares PDB atom *serial numbers*; the verifier was comparing PyMOL's session-local `index` instead of `ID` (the field that actually carries the file's serial). The two only agreed because both checked-in fixtures number atoms 1..N in file order. Confirmed empirically (renumbered fixture: `index`=[1,2,3], `ID`=[101,102,103]) before fixing. Renamed `_selection_atom_indices` -> `_selection_atom_ids`, switched to `ID`; `_capture_colors` deliberately left on `index` (a same-session self-comparison, confirmed fine). New real-PyMOL test against `tests/data/testdata/renumbered_serials_fixture.pdb` (serials 101-103/201-202). |
| `PR13-SYNC-FIX-UNTESTED` | `verifier.py:354` | P2 | The previous round's `cmd.sync()` fix had no test -- a deleted `sync()` call broke nothing observable in CI. Added call-order logging to `test_generate.py`'s `_WrongColorCmd` fake and a new test asserting `sync()` runs both between the plan's `do()` calls and the next state read, and again after `inject_after_execution`. |
| `PR13-PRIVATE-CROSS-MODULE-IMPORT` | `generate.py:33` | P3 | `gold_case.py`'s underscore-prefixed `_required_string` was already part of `generate.py`'s de facto public surface across seven call sites. Renamed to `required_string`. |

Both new tests verified by mutation: reverting `ID` back to `index` makes
the new chain-membership test fail; removing both `sync()` calls makes the
new call-order test fail. Both mutations independently redone by the
reviewing agent, not just self-attested. Full validation at the fix
commit: `bazel build //...`/`bazel test //...` pass (18/18 targets;
`//tests/data:generate` 10->11, `//tests/data:gold_case_verifier` 9->10),
ruff, pyrefly, and dependency boundaries all pass. `codev task check`
returned `ok_machine_review_complete`; PR re-marked ready, each comment
thread replied to individually.

## Review

- Reviewer: Hannah Kullik (`kullik01`)
- Reviewed commits: `47a685d7246165c0732a1e2a684915365ccb4e05` (4 comments, addressed at `7c2e37957ddeb383f71e39b3e17c3d24b9cadb0a`); `bb5fe47708180a05a114c50a37a3071ede98dcdd` (3 more comments, addressed at `4b0de2391b9532a5d1d5136bc1e242fa6421304f`); approved at `8cce4cac2e21b85b8fd5c1db5a6895edf66e535b`
- Independent commands and reports: not separately itemized in this receipt
  beyond the two `CHANGES_REQUESTED` rounds above and the GitHub approval
  record; the `lightweight-reviewer` machine pass independently redid both
  mutation tests for the second round (see that section above).
- Verdict: APPROVED -- GitHub review state `APPROVED`, submitted
  2026-09-07T22:50:28Z, at head commit
  `8cce4cac2e21b85b8fd5c1db5a6895edf66e535b`; PR #13 merged as
  `59748e677a97b54432e857c4307d78369badeec7` at 2026-09-07T23:56:28Z. No
  unresolved blocking finding.
- Findings and resolution: All 7 of Hannah's comments across both review rounds addressed and replied to individually; see tables above

## W2-00 discharges this item's cross-cutting flag

This item's "Outer-loop finding" section (above) flagged, but explicitly
did not fix, the identical real-PyMOL exit-code defect in
`tests/integration/test_real_pymol_command.py` (H-01's file, outside this
item's allowed scope). [W2-00](../../task/W2-00/implementation-plan.md)
fixed that file with the same `os._exit` + explicit-flush pattern this
item established, and empirically reproduced both the defect (Bazel
`PASSED` despite a failing pytest assertion) and its fix (Bazel `FAILED`
with the failure preserved in the log; `PASSED`, 5/5 once restored) on that
file specifically -- see `H-01.md`'s "W2-00 follow-up" section and
[`combined.md`](combined.md) for the exact commands and results. This
item's own historical numbers above are unchanged by this note.
