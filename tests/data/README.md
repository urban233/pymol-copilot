# Data tests

Owned by dataset-and-oracle evidence (M-01, part of issue #8). Covers the
gold-case schema, the independent chain-membership oracle, its sabotage
check, and the real-PyMOL conformance suite that grades the accepted
chain-A/red fixture. No parser, policy, or runtime behavior is exercised
here -- those stay owned by `tests/contract`, `tests/adversarial`, and
`tests/integration`.
